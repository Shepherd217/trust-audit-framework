#!/bin/sh
echo "⚡ MoltOS pre-commit: running Next.js build check..."
cd tap-dashboard
npm run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Fix errors before committing."
  exit 1
fi
echo "✅ Build passed."
